# Quick Commerce Scraper - Python Dependencies
# Install with: pip install -r requirements.txt

# Web Scraping & Browser Automation
playwright==1.40.0          # Browser automation for dynamic content
beautifulsoup4==4.12.2      # HTML parsing
lxml==4.9.3                 # XML/HTML parser (faster than html.parser)

# Async HTTP Requests
aiohttp==3.9.1              # Asynchronous HTTP client/server
aiofiles==23.2.1            # Async file operations

# Data Processing & Analysis
pandas==2.1.4               # Data manipulation and CSV export
numpy==1.26.2               # Numerical computing (pandas dependency)

# Date & Time
python-dateutil==2.8.2      # Date parsing utilities

# Logging & Utilities
colorlog==6.8.0             # Colored terminal logs (optional but nice)

# Testing (Optional)
pytest==7.4.3               # Testing framework
pytest-asyncio==0.21.1      # Async test support

# Note: Standard library packages used (no install needed):
# - json (built-in)
# - csv (built-in)
# - re (built-in)
# - difflib (built-in)
# - os, pathlib (built-in)
# - asyncio (built-in)
# - datetime (built-in)
# - logging (built-in)
# - collections (built-in)
# - typing (built-in)

# Installation Instructions:
# 1. Install Python 3.7 or higher from python.org
# 2. Run: pip install -r requirements.txt
# 3. Install Playwright browsers: playwright install chromium
# 4. Run the scraper: python scraper.py
